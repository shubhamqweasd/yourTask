var taskService = angular.module('app.task.services',[]);

taskService.factory('Task',['$http','$location','$rootScope','$state','$mdToast',function($http,$location,$rootScope,$state,$mdToast){

	return {
		
	}
	
}])


module.exports = taskService;